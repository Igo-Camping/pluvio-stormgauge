# ACTIVE_RUNTIME_DEPENDENCIES — Stormgauge

**Audit branch:** `audit/dead-file-audit-stormgauge`.

This is the **positive list** — files that any cleanup pass must NOT touch. Each is reachable from the live `index.html` execution path or from a documented operator workflow.

---

## Live SPA — entry points and direct loads

| File | Reached from | Notes |
|---|---|---|
| `index.html` | Cloudflare Pages root | The SPA. |
| `styles.css` | `<link rel="stylesheet">` in index.html | |
| `CNAME` | Cloudflare Pages | Custom-domain pinning. |
| `.nojekyll` | Cloudflare Pages | Disables Jekyll. |
| `.gitignore`, `.gitattributes`, `.python-version` | Tooling | Repo config. |

## Loaded synchronously via `<script src>`

| File | Defines | Notes |
|---|---|---|
| `bom_ifd_cache.js` | `window.BOM_IFD_CACHE` | 13.9 MiB — Phase 2 dataset externalisation candidate, but ACTIVE today. |
| `bom_northern_beaches_all_gauges.js` | `window.BOM_NORTHERN_BEACHES_GAUGES` | 7.3 MiB — same. |
| `nsw_lga_boundaries.js` | `window.NSW_LGA_BOUNDARIES` | 1.36 MiB — same. |

## Loaded asynchronously via `fetch()`

| File | Loader | Notes |
|---|---|---|
| `data/pluviometrics_ifd_table.json` | `index.html:4428` `fetch('./data/pluviometrics_ifd_table.json?v=...')` | 6.4 MiB. Generated by `scripts/enrich_ifd.py`. |
| `templates/NBC Rainfall Calculator.xlsm` | `index.html:1368` `RAINFALL_TEMPLATE_URL` | Spreadsheet template. |

## ES modules under `src/modules/`

Loaded by the inline `<script type="module">` block in `index.html`. Each file imports from + exports to other files in the graph; the graph is closed.

| Subtree | Files | Status |
|---|---|---|
| `src/modules/exports/` | `buildExportModel.js`, `exportCsv.js`, `exportHelpers.js`, `exportPng.js`, `exportXlsx.js`, `workbookSheet.js` | All ACTIVE |
| `src/modules/map/` | `mapInit.js`, `mapLayers.js` | All ACTIVE |
| `src/modules/radar/` | `bomRadar.js`, `radarArchiveIndex.js`, `radarAvailability.js`, `radarCumulativeRainfall.js`, `radarGaugeValidationExport.js`, `rainviewerFallback.js` | All ACTIVE |
| `src/modules/stations/` | `stationLoader.js`, `stationMarkers.js` | All ACTIVE |
| `src/modules/ui/` | `controls.js`, `theme.js` | All ACTIVE |

## Test files (Node `--test` entry points)

These are runnable test files. They appear "unreferenced" to the tracer because they're entry points executed via `node --test path/...`, not imported by other code. Each has self-documenting comments showing its run command.

| File | Tests for | Run command |
|---|---|---|
| `src/modules/radar/radarArchiveIndex.test.js` | `radarArchiveIndex.js` | `node --test src/modules/radar/radarArchiveIndex.test.js` |
| `src/modules/radar/radarAvailability.test.js` | `radarAvailability.js` | `node --test src/modules/radar/radarAvailability.test.js` |
| `src/modules/radar/radarCumulativeRainfall.test.js` | `radarCumulativeRainfall.js` | `node --test src/modules/radar/radarCumulativeRainfall.test.js` |
| `src/modules/radar/radarGaugeValidationExport.test.js` | `radarGaugeValidationExport.js` | `node --test src/modules/radar/radarGaugeValidationExport.test.js` |

**Status: ACTIVE_RUNTIME_DEPENDENCY** (runnable tests). Recommendation in a separate report (out of scope for this audit): add a `package.json` with a `test` script that runs all four together so the test surface is discoverable.

## Test fixtures (loaded by code)

| File | Loaded by | Notes |
|---|---|---|
| `reports/fixtures/radar_gauge_validation_fixture.json` | `src/modules/radar/radarGaugeValidationExport.js` (production module) AND `radarGaugeValidationExport.test.js` (test) | ACTIVE. Used by both runtime and test paths. |

## Operator build scripts

| File | Purpose |
|---|---|
| `scripts/enrich_ifd.py` | Builds `data/pluviometrics_ifd_table.json` from a station list. ACTIVE — referenced from `RADAR_VS_GAUGE_SANITY.md` and the IFD enrichment workflow. |

## Brand assets (referenced by index.html)

| File | Reference |
|---|---|
| `Assets/Logos/STORMGAUGE.png` | `index.html:274`, `index.html:296` |

`Assets/Logos/ATMOS.png` and `Assets/Logos/PLUVIOMETRICS.png` are present in the tree but are NOT referenced from `index.html` or any module — they're listed in `NEEDS_CONFIRMATION_CANDIDATES.md` cluster D (unused brand assets).

---

## Total ACTIVE_RUNTIME_DEPENDENCY count: 47 files

This is the protected surface for any future cleanup.
